[package]
name = "mirage-matrix-macros"
version = "0.1.0"
edition = "2021"

[lib]
proc-macro = true

[dependencies]
# مكتبات تشريح وتوليد الكود (The Code Parsers)
syn = { version = "2.0", features = ["full", "extra-traits"] }
quote = "1.0"
proc-macro2 = "1.0"