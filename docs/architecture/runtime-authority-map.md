# Runtime Authority Map (Canonical)

Mapping of runtime authority domains to crates and subsystems. Used by governance metadata and invariants.

See mirage-core::governance for formal enum and types.
